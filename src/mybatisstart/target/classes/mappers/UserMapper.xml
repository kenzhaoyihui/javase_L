<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="moreToMoreTables.mapper.UserMapper">
    <parameterMap id="parameterUserMap" type="moreToMoreTables.pojo.User">
        <parameter property="id"/>
        <parameter property="username"/>
        <parameter property="mobile"/>
    </parameterMap>

    <insert id="insertUser" parameterMap="parameterUserMap">
        insert into user1 (username, mobile) values (#{username}, #{mobile})
    </insert>

    <resultMap id="resultUserMap_1" type="moreToMoreTables.pojo.User">
        <!--<result property="id" column="group_id"/>-->
        <id property="id" column="id"/>
        <!--<result property="groupId" column="group_id"/>-->
        <result property="name" column="name"/>
        <collection property="groups" column="id" select="moreToMoreTables.mapper.UserGroupMapper.getGroupsByUserId"/>
    </resultMap>

    <select id="getUser" resultMap="resultUserMap_1" parameterType="int">
        select * from user1 where id=#{id}
    </select>
</mapper>