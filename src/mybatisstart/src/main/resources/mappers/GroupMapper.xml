<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="moreToMoreTables.mapper.GroupMapper">
    <parameterMap id="parameterGroupMap" type="moreToMoreTables.pojo.Group">
        <parameter property="groupId"/>
        <parameter property="groupName"/>
    </parameterMap>

    <insert id="insertGroup" parameterMap="parameterGroupMap">
        insert into `group` (group_name) values (#{groupName})
    </insert>

    <resultMap id="resultGroupMap_1" type="moreToMoreTables.pojo.Group">
        <id property="groupId" column="group_id"/>
        <!--<result property="id" column="id"/>-->
        <result property="groupName" column="group_name"/>
        <collection property="users" column="group_id" select="moreToMoreTables.mapper.UserGroupMapper.getUsersByGroupId"/>
    </resultMap>

    <select id="getGroup" resultMap="resultGroupMap_1" parameterType="int">
        select * from `group` where group_id=#{groupId}
    </select>
</mapper>