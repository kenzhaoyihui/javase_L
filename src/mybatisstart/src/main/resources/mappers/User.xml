<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="userpost.UserMapper">
    <!-- one user related to more posts, one->more-->
    <resultMap id="resultUserMap" type="userpost.User">
        <id property="id" column="id"/>
        <!--<result property="id" column="user__id"/>-->
        <result property="username" column="username"/>
        <result property="mobile" column="mobile"/>
        <collection property="posts" ofType="userpost.Post">
            <id property="id" column="post_id" javaType="int" jdbcType="INTEGER"/>
            <result property="title" column="title" javaType="String" jdbcType="VARCHAR"/>
            <result property="content" column="content" javaType="String" jdbcType="VARCHAR"/>
        </collection>
    </resultMap>

    <resultMap id="resultPostMap" type="userpost.Post">
        <id property="id" column="post_id"/>
        <!--<result property="id" column="postid"/>-->
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <association property="user" javaType="userpost.User">
            <id property="id" column="userid"/>
            <result property="username" column="username"/>
            <result property="mobile" column="mobile"/>
        </association>
    </resultMap>

    <select id="getUser" resultMap="resultUserMap" parameterType="int">
        select u.*, p.* from user u, post p where u.id=p.userid and id=#{user__id}
    </select>

    <select id="getPosts" resultMap="resultPostMap" parameterType="int">
        select u.*, p.* from user u, post p where u.id=p.userid and p.post_id=#{post_id}
    </select>
</mapper>